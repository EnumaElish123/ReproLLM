# Audit rules

This file is generated from the registered Python rules and built-in profiles.
Run `python scripts/gen_rules_doc.py` after changing either source.

| Rule ID | Category | Default | Level | Status | Effective profiles | Description | Fix |
|---|---|---|---:|---|---|---|---|
| `code.clean_tree` | code | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Tracked files have no uncommitted modifications. | Commit or stash your changes: `git add -A && git commit` or `git stash`. |
| `code.git_commit` | code | CRITICAL | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | HEAD points at a committed revision (no unborn branch). | Commit the current state: `git add -A && git commit -m 'experiment'`. |
| `code.git_repo` | code | CRITICAL | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The experiment code lives in a git repository. | Run `git init` and commit the experiment code so its state is recorded. |
| `code.no_untracked` | code | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | No untracked, non-ignored files are left in the tree. | Commit them, delete them, or add them to .gitignore. |
| `code.remote_recorded` | code | INFO | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | An origin remote records where this code is published. | Run `git remote add origin <url>` so the experiment's code source is recorded. |
| `code.submodules_initialized` | code | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | All declared submodules have a checked-out commit. | Run `git submodule update --init --recursive`. |
| `consistency.custom_fields` | consistency | project rule | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Observed custom values agree with the manifest and accepted project rules. | Reconcile custom.* in reprollm.yaml with bindings in .reprollm/project-rules.yaml. |
| `consistency.env_vs_lock` | consistency | WARNING | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Runtime LLM-critical package versions agree with the lock. | Use the environment.packages versions recorded in reprollm.lock and rerun. |
| `consistency.file_hashes` | consistency | CRITICAL | 2 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Declared file hashes agree between the lock, working tree, and latest run. | Reconcile reprollm.lock prompts/files hashes with the intended files and rerun. |
| `consistency.generation_params` | consistency | CRITICAL | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Observed generation parameters agree with their manifest declarations. | Reconcile generation.* in reprollm.yaml with the declared runtime bindings. |
| `consistency.lock_fresh` | consistency | WARNING | 2 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The lock matches the manifest and accepted project rules. | Run `reprollm lock` to refresh reprollm.lock after declaration changes. |
| `consistency.model_identity` | consistency | CRITICAL | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Observed model identities agree with the manifest and lock. | Reconcile models.<role>.id and revision in reprollm.yaml and reprollm.lock. |
| `dataset.declared` | dataset | INFO | 1 | implemented | evaluation, finetuning, llm_judge, safety | At least one dataset role is declared. | Declare datasets.<role> in reprollm.yaml. |
| `dataset.local_files_hashed` | dataset | CRITICAL | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every declared local dataset file has a recorded hash. | Run `reprollm lock` to hash datasets.<role>.files in reprollm.lock. |
| `dataset.preprocessing_declared` | dataset | WARNING | 1 | implemented | evaluation, finetuning, llm_judge, safety | Every dataset has declared preprocessing. | Set datasets.<role>.preprocessing in reprollm.yaml. |
| `dataset.revision_pinned` | dataset | WARNING | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every Hugging Face dataset has an exact resolved revision. | Run `reprollm lock` to resolve datasets.<role>.revision in reprollm.lock. |
| `dataset.sampling_seed_declared` | dataset | WARNING | 1 | implemented | evaluation, llm_judge, safety | Every dataset with a sample count has a sampling seed. | Set datasets.<role>.sampling.seed in reprollm.yaml. |
| `dataset.split_declared` | dataset | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every Hugging Face dataset has a declared split. | Set datasets.<role>.split in reprollm.yaml. |
| `dataset.subset_declared` | dataset | INFO | 1 | implemented | evaluation, llm_judge, safety | Every Hugging Face dataset has a declared subset. | Set datasets.<role>.subset in reprollm.yaml. |
| `env.dependency_manifest_present` | env | CRITICAL | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | A dependency manifest declares the Python environment. | Add a pyproject.toml, requirements*.txt, or environment.yml that declares the dependencies of this experiment. |
| `env.llm_critical_deps_pinned` | env | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every LLM-critical dependency in use is pinned to an exact version. | Pin the package exactly (see the message for the suggested version). |
| `env.lockfile_present` | env | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | A lockfile (or fully pinned requirements) records exact versions. | Commit a uv.lock/poetry.lock, or pin every dependency with `==` in requirements.txt so the environment can be rebuilt exactly. |
| `env.python_version_declared` | env | WARNING | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The required Python version is declared. | Add `requires-python` to pyproject.toml, a .python-version file, or a `python=3.x` entry in environment.yml. |
| `env.reprollm_initialized` | env | INFO | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The repository is initialized for ReproLLM. | Run `reprollm init` to create reprollm.yaml and .reprollm/. |
| `env.secret_files_ignored` | env | CRITICAL | 0 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Secret-bearing files are not present or are gitignored. | Remove the secret file from the repository, rotate the secret, and add the pattern to .gitignore (keep only .env.example-style templates). |
| `eval.aggregation_declared` | eval | WARNING | 1 | implemented | evaluation, llm_judge, safety | The metric aggregation method is declared. | Set evaluation.aggregation in reprollm.yaml. |
| `eval.definitions_declared` | eval | WARNING | 1 | implemented | safety | Refusal or attack success is defined for safety evaluation. | Set evaluation.definitions.refusal or evaluation.definitions.asr in reprollm.yaml. |
| `eval.metric_implementation_referenced` | eval | WARNING | 1 | implemented | evaluation, llm_judge, safety | Each metric references its implementation. | Set evaluation.metrics.<index>.implementation to a relative file or pkg==version in reprollm.yaml. |
| `eval.metrics_declared` | eval | WARNING | 1 | implemented | evaluation, llm_judge, safety | Evaluation metrics are declared. | Declare evaluation.metrics in reprollm.yaml. |
| `eval.query_budget_declared` | eval | WARNING | 1 | implemented | safety | The evaluation query budget is declared. | Set evaluation.query_budget in reprollm.yaml. |
| `eval.repetitions_declared` | eval | WARNING | 1 | implemented | evaluation, llm_judge, safety | The number of evaluation repetitions is declared. | Set evaluation.repetitions in reprollm.yaml. |
| `exec.command_declared` | exec | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The command that reproduces the experiment is declared. | Set execution.command in reprollm.yaml, e.g. `execution: {command: 'python eval.py --config configs/eval.yaml'}`. |
| `exec.profile_detection_mismatch` | exec | INFO | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Declared profiles and detected signals agree. | Review experiment.profiles in reprollm.yaml against the detection evidence; add or remove a profile only to describe the selected experiment. Capabilities used by other repository workflows need no manifest change. |
| `exec.run_recorded` | exec | INFO | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | At least one run record captures what actually executed. | Wrap your command with `reprollm run -- <command>` to record runtime truth. |
| `exec.seed_declared` | exec | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | A random seed is declared somewhere (execution/generation/training). | Set execution.seed (or generation.seed / training.seed) in reprollm.yaml. |
| `gen.backend_config_declared` | gen | WARNING | 1 | implemented | evaluation, inference, llm_judge, safety | The vLLM backend configuration is declared. | Set inference.dtype, inference.tensor_parallel_size, inference.quantization and inference.gpu_memory_utilization in reprollm.yaml. |
| `gen.backend_declared` | gen | WARNING | 1 | implemented | evaluation, inference, llm_judge, safety | The inference backend is declared. | Set inference.backend in reprollm.yaml. |
| `gen.backend_version_locked` | gen | WARNING | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The non-API inference backend has an exact locked version. | Run `reprollm lock` to record inference.version in reprollm.lock. |
| `gen.params_declared` | gen | WARNING | 1 | implemented | evaluation, inference, llm_judge, safety | Generation temperature, top_p and max_tokens are declared. | Set generation.temperature, generation.top_p and generation.max_tokens in reprollm.yaml. |
| `gen.seed_declared` | gen | WARNING | 1 | implemented | evaluation, inference, llm_judge, safety | Sampled generation has a declared random seed. | Set generation.seed in reprollm.yaml when sampling is enabled. |
| `gen.stop_declared` | gen | INFO | 1 | implemented | evaluation, inference, llm_judge, safety | Generation stop sequences are explicitly declared. | Set generation.stop in reprollm.yaml; use [] when no stop sequences are needed. |
| `judge.model_declared` | judge | CRITICAL | 1 | implemented | llm_judge | The judge model role is declared. | Declare models.judge or the role named by evaluation.judge.model_ref in reprollm.yaml. |
| `judge.params_declared` | judge | CRITICAL | 1 | implemented | llm_judge | Judge temperature and max_tokens are declared. | Set evaluation.judge.params.temperature and evaluation.judge.params.max_tokens in reprollm.yaml. |
| `judge.pinnability_recorded` | judge | WARNING | 2 | stub, arrives in 0.2.0 | llm_judge | The declared judge model has an explicit pinnability record. | Run `reprollm lock` to record pinnability for evaluation.judge.model_ref in reprollm.lock. |
| `judge.prompt_declared` | judge | CRITICAL | 1 | implemented | llm_judge | The judge prompt role is declared. | Declare prompts.judge or the role named by evaluation.judge.prompt_ref in reprollm.yaml. |
| `judge.prompt_hashed` | judge | WARNING | 2 | stub, arrives in 0.2.0 | llm_judge | The declared judge prompt has a recorded content hash. | Run `reprollm lock` to hash the evaluation.judge.prompt_ref role in reprollm.lock. |
| `judge.repetitions_declared` | judge | WARNING | 1 | implemented | llm_judge | The number of judge repetitions is declared. | Set evaluation.judge.repetitions in reprollm.yaml. |
| `model.adapter_declared` | model | WARNING | 1 | implemented | finetuning | The detected adapter is declared on a model. | Set models.<role>.adapter in reprollm.yaml to describe the detected adapter. |
| `model.chat_template_hashed` | model | WARNING | 2 | stub, arrives in 0.2.0 | evaluation, inference, llm_judge, safety | Every Hugging Face or local model has a chat-template hash or recorded absence. | Run `reprollm lock` to record models.<role>.chat_template in reprollm.lock. |
| `model.dtype_declared` | model | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every non-API model has a declared dtype. | Set models.<role>.dtype or inference.dtype in reprollm.yaml. |
| `model.primary_declared` | model | CRITICAL | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | The primary model identity is declared. | Set models.primary.id in reprollm.yaml. |
| `model.provider_known` | model | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every model uses a known provider. | Set models.<role>.provider to a supported provider in reprollm.yaml. |
| `model.quantization_declared` | model | INFO | 1 | implemented | evaluation, inference, llm_judge, safety | Every non-API model has declared quantization. | Set models.<role>.quantization or inference.quantization in reprollm.yaml. |
| `model.revision_pinned` | model | CRITICAL | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every model has a resolved identity or an explicit API pinnability record. | Run `reprollm lock` to resolve models.<role>.revision in reprollm.lock. |
| `model.tokenizer_pinned` | model | WARNING | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every Hugging Face model has an exact tokenizer revision. | Run `reprollm lock` to resolve models.<role>.tokenizer.revision in reprollm.lock. |
| `model.trust_remote_code_declared` | model | WARNING | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Detected remote-code trust behavior is explicitly declared. | Set models.<role>.trust_remote_code to true or false in reprollm.yaml to match the selected execution path. |
| `privacy.attack_config_declared` | privacy | WARNING | 1 | implemented | privacy | The declared privacy attack has a method and query budget. | Set privacy.attack.method and privacy.attack.query_budget in reprollm.yaml. |
| `privacy.mechanism_declared` | privacy | CRITICAL | 1 | implemented | privacy | The privacy mechanism has a name and nonempty parameters. | Set privacy.mechanism.name and nonempty privacy.mechanism.params in reprollm.yaml. |
| `privacy.metrics_declared` | privacy | WARNING | 1 | implemented | privacy | Privacy metrics are declared. | Declare privacy.metrics in reprollm.yaml. |
| `privacy.threat_model_declared` | privacy | CRITICAL | 1 | implemented | privacy | The privacy threat model is declared. | Set privacy.threat_model in reprollm.yaml. |
| `prompt.declared` | prompt | WARNING | 1 | implemented | evaluation, inference, llm_judge, safety | At least one prompt role is declared. | Declare prompts.<role>.path or prompts.<role>.text in reprollm.yaml. |
| `prompt.few_shot_declared` | prompt | INFO | 1 | implemented | evaluation, inference, llm_judge, safety | Few-shot configuration is explicitly declared. | Set prompts.<role>.few_shot in reprollm.yaml; use n: 0 for zero-shot prompts. |
| `prompt.file_exists` | prompt | CRITICAL | 1 | implemented | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every referenced prompt file exists. | Set prompts.<role>.path to an existing file in reprollm.yaml. |
| `prompt.hashed` | prompt | WARNING | 2 | stub, arrives in 0.2.0 | core, evaluation, finetuning, inference, llm_judge, privacy, safety | Every prompt role has a recorded content hash. | Run `reprollm lock` to hash prompts.<role> in reprollm.lock. |
| `train.hyperparameters_declared` | train | CRITICAL | 1 | implemented | finetuning | Learning rate, batch size and training duration are declared. | Set training.learning_rate, training.batch_size and either training.epochs or training.max_steps in reprollm.yaml. |
| `train.lora_config_complete` | train | CRITICAL | 1 | implemented | finetuning | One adapter declares LoRA rank, alpha and target_modules together. | Set models.<role>.adapter.rank, models.<role>.adapter.alpha and models.<role>.adapter.target_modules on the same adapter in reprollm.yaml. |
| `train.method_declared` | train | WARNING | 1 | implemented | finetuning | The training method is declared. | Set training.method in reprollm.yaml. |
| `train.optimizer_declared` | train | WARNING | 1 | implemented | finetuning | The optimizer and scheduler are declared. | Set training.optimizer and training.scheduler in reprollm.yaml. |
| `train.precision_declared` | train | WARNING | 1 | implemented | finetuning | Training precision is declared. | Set training.precision in reprollm.yaml. |

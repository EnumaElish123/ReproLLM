# rules are completed in M3
schema_version: 1
name: llm_judge
description: LLM-as-a-judge evaluation (judge model, prompt, and parameters).
extends: [evaluation]
rules: []
required_fields:
  - models.judge.id
  - prompts.judge.path
  - evaluation.judge.params.temperature
detect:
  imports: []
  dependencies: []
  keywords: [judge, llm-as-a-judge, llm_judge, rubric, grader]
  files: []

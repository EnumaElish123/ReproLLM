# Presence rules and severity overrides follow spec §6.1.
schema_version: 1
name: safety
description: Safety and red-team evaluations (definitions and query budgets).
extends: [evaluation]
rules:
  - eval.definitions_declared
  - eval.query_budget_declared
required_fields:
  - evaluation.definitions.refusal
  - evaluation.definitions.asr
severity_overrides:
  eval.definitions_declared: CRITICAL
drift_overrides: {}
detect:
  imports: []
  dependencies: []
  keywords: [jailbreak, attack success rate, asr, refusal, harmbench, advbench, red team, red-team]
  files: []

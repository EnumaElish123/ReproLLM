# Presence rules and severity overrides follow spec §6.1.
schema_version: 1
name: privacy
description: Privacy-preserving LLM experiments (threat model and mechanism).
extends: [core]
rules:
  - privacy.threat_model_declared
  - privacy.mechanism_declared
  - privacy.metrics_declared
  - privacy.attack_config_declared
required_fields:
  - privacy.threat_model
  - privacy.mechanism.name
severity_overrides: {}
drift_overrides: {}
detect:
  imports: []
  dependencies: []
  keywords: [differential privacy, epsilon, membership inference, threat model, privacy budget, dp-sgd, dp_sgd]
  files: []

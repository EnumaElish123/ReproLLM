# hf_api corpus

Recorded (hand-written, minimal) Hugging Face Hub API responses used by respx
mocks in M4: `models/<repo>/revision_main.json` (`sha` + `siblings[].rfilename`)
and small file bodies (`config.json`, `tokenizer_config.json`, …). Never fetched
from the network.

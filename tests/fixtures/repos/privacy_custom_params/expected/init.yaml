# reprollm.yaml — what this experiment intends to run (spec §3).
# Generated by `reprollm init` from detected signals.
# Keys marked TODO are required by your profiles; commented blocks are examples.
# The manifest stays loadable: `reprollm audit .` validates it.

schema_version: 1

project:
  name: privacy_custom_params

experiment:
  profiles: [privacy, safety]


models:
  primary:
    id: meta-llama/Llama-3.1-8B-Instruct  # detected: method.py:23


datasets:
  eval:
    id:  # TODO


generation:
  temperature:  # TODO
  max_tokens:  # TODO


inference:
  backend:  # TODO


evaluation:
  metrics: []  # TODO: list of {name, implementation}
  # definitions:
  #   refusal: definitions/refusal.txt  # text or relative path
  # definitions:
  #   asr: definitions/asr.txt  # text or relative path


privacy:
  threat_model:  # TODO
  # mechanism:
  #   name: orthogonal_noise
  #   params: {alpha: 0.25}


# --- Example sections (uncomment and edit what applies) ----------------------

# prompts:
#   system:
#     path: prompts/system.txt
#     format: plain


# training:
#   method: lora
#   learning_rate: 0.0001
#   batch_size: 8


# execution:
#   command: python eval.py --config configs/eval.yaml
#   seed: 42
#   config_files: [configs/eval.yaml]



# nvidia_smi corpus

Canned `nvidia-smi` outputs for hardware capture tests (M5): query CSV for one
and multiple GPUs, driver query, header text with the CUDA version line, and a
`missing` case simulating a absent binary.
